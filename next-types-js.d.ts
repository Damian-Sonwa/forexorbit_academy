/**
 * Shim: Next.js generated `.next/types/validator.ts` imports `next/types.js`, which is not
 * a published entry in the `next` package. Re-export from `next` so typecheck succeeds.
 */
export type { NextApiHandler } from 'next';
